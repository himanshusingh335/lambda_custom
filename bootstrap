#!/bin/sh
#
# AWS Lambda Custom Runtime Bootstrap
#
# This script is the entry point for the Lambda custom runtime.
# Lambda invokes this executable to start the runtime process.
#

# Change to the Lambda task root directory
cd $LAMBDA_TASK_ROOT

# Execute the Python runtime
# Using exec to replace the shell process with Python (better resource management)
# For provided.al2, use system Python
exec /usr/bin/python3 runtime.py
